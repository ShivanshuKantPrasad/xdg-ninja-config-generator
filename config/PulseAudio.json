{
    "name": "PulseAudio",
    "files": [
        {
            "path": "~/.esd_auth",
            "movable": true,
            "help": "Very likely generated by the module-esound-protocol-unix.so module.  It can be configured to use a different location but it makes much more sense to just comment out this module in /etc/pulse/default.pa or \"$XDG_CONFIG_HOME\"/pulse/default.pa.\n"
        }
    ]
}   ]
}