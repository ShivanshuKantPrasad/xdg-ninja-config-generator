{
    "name": "mathomatic",
    "files": [
        {
            "path": "~/.mathomaticrc",
            "movable": false,
            "help": "History can be moved by using rlwrap mathomatic -r with the RLWRAP_HOME environment set appropriately.\n"
        },
        {
            "path": "~/.matho_history",
            "movable": false,
            "help": "History can be moved by using rlwrap mathomatic -r with the RLWRAP_HOME environment set appropriately.\n"
        }
    ]
}